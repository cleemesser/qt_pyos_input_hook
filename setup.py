from distutils.core import setup
import os

#    requires=["PyQt4 (>4.3)",],

setup(
    name='qt_pyos_input_hook',
    version="0.1",
    author='Chris Lee-Messer',
    packages=["qt_pyos_input_hook" ],
    # ext_modules = [] # need to figure out how to hook qmake into an extension?
    )
